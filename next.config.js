/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['mkhinvestment.com', 'real-estate-nigeria.beforward.jp']
  }
}

module.exports = nextConfig
